import { router } from 'expo-router'; import { useQuery,useMutation,useQueryClient } from '@tanstack/react-query'; import { useState } from 'react'; import { StyleSheet,Text,TextInput,View } from 'react-native'; import { Button,Card,ErrorView,LoadingView } from '@/src/components/ui'; import { Screen,TopBar } from '@/src/components/layout'; import { colors,spacing,typography } from '@/src/design/tokens'; import { inviteApi } from '@/src/features/student/invite/api'; import { useInviteSessionStore } from '@/src/features/student/invite/session-store';
export default function StudentProgressScreen(){const s=useInviteSessionStore(x=>x.session);const c=useQueryClient();const [weight,setWeight]=useState('');const q=useQuery({queryKey:['student','weight'],queryFn:()=>inviteApi.weight(s!.accessToken),enabled:Boolean(s)});const add=useMutation({mutationFn:()=>inviteApi.addWeight(s!.accessToken,Number(weight.replace(',','.'))),onSuccess:()=>{setWeight('');c.invalidateQueries({queryKey:['student','weight']})}});if(!s){router.replace('/login');return null;}if(q.isLoading)return <LoadingView message="Carregando seu progresso…"/>;if(q.isError)return <ErrorView message={q.error.message} onRetry={()=>q.refetch()}/>;return <Screen style={styles.page}><TopBar eyebrow="PROGRESSO" title="Seu peso" onBack={()=>router.back()}/><Card style={styles.card}><Text style={styles.title}>Registrar nova medida</Text><View style={styles.row}><TextInput value={weight} onChangeText={setWeight} keyboardType="decimal-pad" placeholder="Peso em kg" placeholderTextColor={colors.textMuted} style={styles.input}/><Button loading={add.isPending} disabled={!weight.trim()} onPress={()=>add.mutate()}>Salvar</Button></View></Card><View style={styles.list}>{q.data!.slice().reverse().map((e)=><Card key={e.id} style={styles.entry}><Text style={styles.value}>{e.weightKg} kg</Text><Text style={styles.copy}>{new Intl.DateTimeFormat('pt-BR',{dateStyle:'medium'}).format(new Date(e.recordedAt))}</Text></Card>)}</View></Screen>}
const styles=StyleSheet.create({page:{paddingVertical:spacing.xl,gap:spacing.md},card:{gap:spacing.md},title:{...typography.headingMD,color:colors.textPrimary},row:{flexDirection:'row',gap:spacing.sm,alignItems:'center'},input:{...typography.bodyMD,color:colors.textPrimary,borderWidth:1,borderColor:colors.border,borderRadius:12,padding:spacing.md,flex:1},list:{gap:spacing.sm},entry:{gap:spacing.xs},value:{...typography.metricXL,color:colors.signalGreen},copy:{...typography.bodyMD,color:colors.textSecondary}});
