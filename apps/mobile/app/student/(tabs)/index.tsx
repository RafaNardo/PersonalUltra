import { StudentWaitingHome } from '@/src/features/student/invite/waiting-home';

/** Existing Student home surface, now hosted by the independent tab tree. */
export default function StudentHomeRoute() {
  return <StudentWaitingHome />;
}
